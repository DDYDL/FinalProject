using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JySpaceData : MonoBehaviour
{
    public static bool IsRegi = false;

    public static void JySendSpace() {
        IsRegi = true;
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
